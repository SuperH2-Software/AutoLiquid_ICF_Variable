<UserControl x:Class="AutoLiquid_ICF_Variable.UserControls.ControlTemplateTip"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:metro="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="600">
    <Grid>
        <Grid Background="{StaticResource MahApps.Brushes.Accent}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <DockPanel  Grid.Row="0">
                <Label x:Name="LabelTitle" Content="{StaticResource TemplateTips}" Foreground="White" FontWeight="Bold" FontSize="15"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,5,0">
                    <Label Content="{StaticResource TipStartPos}" VerticalContentAlignment="Center"/>
                    <metro:SplitButton 
                        x:Name="SplitButtonTipsBoxPos"
                        HorizontalContentAlignment="Left"
                        HorizontalAlignment="Center"
                        VerticalContentAlignment="Center"
                        Width="60"
                        SelectedIndex="0"
                        VerticalAlignment="Center" 
                        x:FieldModifier="public"/>
                </StackPanel>
            </DockPanel>
            <Border Grid.Row="1" Height="2" Background="Gray" Margin="10,0,10,0"/>
            <StackPanel Grid.Row="2" x:Name="StackPanelTipsBox" Margin="20,5"/>
        </Grid>
    </Grid>
</UserControl>
